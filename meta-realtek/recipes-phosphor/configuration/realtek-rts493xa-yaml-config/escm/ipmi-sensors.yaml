0x01:
    sensorType: 0x01
    path: /xyz/openbmc_project/sensors/temperature/Ambient_Temp1
    sensorReadingType: 0x01
    multiplierM: 1
    offsetB: 0
    bExp: 0
    # Result exponent field in Type 1 SDR(2's complement, signed)
    rExp: 0
    # Applies for analog sensors, the actual reading value for the sensor is
    # Value * 10^N
    # scale: -3
    # Indicate Analog Data Format, Rate unit, Modifier unit and Percentage
    sensorUnits1: 0x80
    unit: xyz.openbmc_project.Sensor.Value.Unit.DegreesC
    mutability: Mutability::Read
    serviceInterface: org.freedesktop.DBus.Properties
    readingType: readingData
    sensorNamePattern: nameLeaf
    interfaces:
        xyz.openbmc_project.Sensor.Value:
            Value:
                Offsets:
                    0xFF:
                        type: double

0x02:
    sensorType: 0x01
    path: /xyz/openbmc_project/sensors/temperature/Ambient_Temp2
    sensorReadingType: 0x01
    multiplierM: 1
    offsetB: 0
    bExp: 0
    # Result exponent field in Type 1 SDR(2's complement, signed)
    rExp: 0
    # Applies for analog sensors, the actual reading value for the sensor is
    # Value * 10^N
    # scale: -3
    # Indicate Analog Data Format, Rate unit, Modifier unit and Percentage
    sensorUnits1: 0x80
    unit: xyz.openbmc_project.Sensor.Value.Unit.DegreesC
    mutability: Mutability::Read
    serviceInterface: org.freedesktop.DBus.Properties
    readingType: readingData
    sensorNamePattern: nameLeaf
    interfaces:
        xyz.openbmc_project.Sensor.Value:
            Value:
                Offsets:
                    0xFF:
                        type: double
